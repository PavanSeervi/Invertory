<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Item</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            /* Light background for better contrast */
        }

        .container {
            margin-top: 50px;
            border-radius: 5px;
            padding: 20px;
            background-color: #ffffff;
            /* White background for the form */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            /* Subtle shadow */
        }

        h1 {
            margin-bottom: 20px;
            color: #007bff;
            /* Bootstrap primary color */
        }

        .btn-primary {
            margin-top: 20px;
            /* Space above the button */
            background-color: #28a745;
            /* Green color for update button */
            border: none;
        }

        .btn-primary:hover {
            background-color: #218838;
            /* Darker green on hover */
        }

        .cancel-link {
            margin-top: 10px;
            display: inline-block;
            color: #dc3545;
            /* Red color for cancel link */
        }

        .cancel-link:hover {
            color: #c82333;
            /* Darker red on hover */
        }

        .form-label {
            font-weight: bold;
            /* Bold labels for better readability */
        }

        .icon {
            margin-right: 5px;
            /* Spacing for icons */
        }

        .back-button {
            position: absolute;
            top: 20px;
            right: 20px;
            color: #007bff;
            font-size: 1.5rem;
            text-decoration: none;
        }

        .back-button:hover {
            color: #0056b3;
            /* Darker blue on hover */
        }
    </style>
</head>

<body>
    <div class="container position-relative">
        <a href="/" class="back-button" title="Back to Home"><i class="fas fa-arrow-left"></i></a>
        <h1><i class="fas fa-edit icon"></i>Edit Item</h1>

        <form action="/api/items/<%= item._id %>" method="POST">
            <div class="form-group">
                <label for="name" class="form-label"><i class="fas fa-tag icon"></i>Item Name</label>
                <input type="text" id="name" name="name" class="form-control" value="<%= item.name %>" required>
            </div>
            <div class="form-group">
                <label for="price" class="form-label"><i class="fas fa-rupee-sign icon"></i>Price (₹)</label>
                <input type="number" id="price" name="price" class="form-control" value="<%= item.price %>" required>
            </div>
            <div class="form-group">
                <label for="sku" class="form-label"><i class="fas fa-barcode icon"></i>SKU</label>
                <input type="text" id="sku" name="sku" class="form-control" value="<%= item.sku %>" required>
            </div>
            <div class="form-group">
                <label for="description" class="form-label"><i class="fas fa-info-circle icon"></i>Description</label>
                <textarea id="description" name="description" class="form-control" rows="4"
                    required><%= item.description %></textarea>
            </div>
            <button type="submit" class="btn btn-primary"><i class="fas fa-save icon"></i>Update Item</button>
        </form>

        <a href="/" class="btn btn-secondary cancel-link"><i class="fas fa-times icon"></i>Cancel</a>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>