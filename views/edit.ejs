<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Item</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        body {
            background-color: #f8f9fa;
        }

        .container {
            margin-top: 50px;
            border-radius: 5px;
            padding: 20px;
            background-color: #ffffff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            margin-bottom: 20px;
            color: #007bff;
        }

        .btn-primary {
            margin-top: 20px;
            background-color: #28a745;
            border: none;
        }

        .btn-primary:hover {
            background-color: #218838;
        }

        .cancel-link {
            margin-top: 10px;
            display: inline-block;
            color: #dc3545;
        }

        .cancel-link:hover {
            color: #c82333;
        }

        .form-label {
            font-weight: bold;
        }

        .icon {
            margin-right: 5px;
        }

        .back-button {
            position: absolute;
            top: 20px;
            right: 20px;
            color: #007bff;
            font-size: 1.5rem;
            text-decoration: none;
        }

        .back-button:hover {
            color: #0056b3;
        }
    </style>
</head>

<body>
    <div class="container position-relative">
        <a href="/" class="back-button" title="Back to Home"><i class="fas fa-arrow-left"></i></a>
        <h1><i class="fas fa-edit icon"></i>Edit Item</h1>

        <form id="searchForm" action="/api/items/search" method="GET">
            <div class="form-group">
                <label for="sku" class="form-label"><i class="fas fa-barcode icon"></i>Search by Product Code</label>
                <input type="text" id="sku" name="sku" class="form-control" placeholder="Enter Product Code">
            </div>
            <button type="submit" class="btn btn-primary"><i class="fas fa-search icon"></i>Search</button>
        </form>

        <form id="editForm" action="/api/items/<%= item._id %>" method="POST" class="mt-4">
            <div class="form-group">
                <label for="name" class="form-label"><i class="fas fa-tag icon"></i>Item Name</label>
                <input type="text" id="name" name="name" class="form-control" value="<%= item.name %>" required>
            </div>
            <div class="form-group">
                <label for="price" class="form-label"><i class="fas fa-rupee-sign icon"></i>Price (₹)</label>
                <input type="number" id="price" name="price" class="form-control" value="<%= item.price %>" required>
            </div>
            <div class="form-group">
                <label for="sku" class="form-label"><i class="fas fa-barcode icon"></i>Product Code</label>
                <input type="text" id="sku" name="sku" class="form-control" value="<%= item.sku %>" required>
            </div>
            <div class="form-group">
                <label for="description" class="form-label"><i class="fas fa-info-circle icon"></i>Description</label>
                <textarea id="description" name="description" class="form-control" rows="4"
                    required><%= item.description %></textarea>
            </div>
            <button type="submit" class="btn btn-primary"><i class="fas fa-save icon"></i>Update Item</button>
        </form>

        <a href="/" class="btn btn-secondary cancel-link"><i class="fas fa-times icon"></i>Cancel</a>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>